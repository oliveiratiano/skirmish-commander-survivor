using UnityEngine;

public class ArenaBootstrap : MonoBehaviour
{
    // Scene bootstrap — systems self-register in Awake.
    // DraftUI.StartBattle() triggers GameFlowManager which starts the wave.
}
